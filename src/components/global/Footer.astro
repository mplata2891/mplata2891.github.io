---
// src/components/global/Footer.astro
import { FOOTER } from "../../data/footer.js";

const currentYear = new Date().getFullYear();
---

<footer class="w-full border-t border-[var(--color-border-subtle)] bg-void mt-32 py-8 px-6 lg:px-32 relative z-10">
  <div class="max-w-6xl mx-auto flex flex-col md:flex-row justify-between items-center gap-6">
    
    {/* System Status Readout */}
    <div class="flex items-center gap-3">
      {/* FIXED: Swapped manual circle for the global hardware node class */}
      <div class="status-dot status-dot--accent"></div>
      <p class="font-special-ui text-xs text-secondary-color uppercase tracking-widest">
        {FOOTER.statusText}
      </p>
    </div>

    {/* External Comm Links */}
    <div class="flex gap-6">
      {FOOTER.commLinks.map((link) => (
        <a 
          href={link.url} 
          target={link.url.startsWith('http') ? "_blank" : "_self"} 
          rel={link.url.startsWith('http') ? "noopener noreferrer" : ""} 
          class="font-special-ui text-xs text-secondary-color hover:text-accent transition-colors glitch-hover"
        >
          {link.label}
        </a>
      ))}
    </div>

    {/* Copyright & Versioning */}
    <p class="font-special-ui text-xs text-secondary-color opacity-50">
      © {currentYear} {FOOTER.copyrightInitials} // {FOOTER.systemVersion}
    </p>

  </div>
</footer>